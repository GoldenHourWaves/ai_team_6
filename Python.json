{
    // Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
    // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
    // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
    // same ids are connected.
    // Example:
    // "Print to console": {
    // 	"prefix": "log",
    // 	"body": [
    // 		"console.log('$1');",
    // 		"$2"
    // 	],
    // 	"description": "Log output to console"
    // }
    "Python_Print": {
        "prefix": "pprint",
        "body": [
            "print('$1')"
        ],
        "description": "Log output to console"
    },
    "Python_Printf": {
        "prefix": "fstring",
        "body": [
            "print(f'$1')"
        ],
        "description": "fstring Log output to console"
    },
    "Python_Word_Cloud": {
        "prefix": "pythonwordcloud",
        "body": [
            "# 한국어 텍스트 데이터 분석 및 워드 클라우드 생성",
            "# 분석할 파일명 1 : ./data/datafile.csv",
            "# cvs 파일에서 text 컬럼을 읽어서 형태소 분석기(Okt)로 토크나이징",
            "# 문장을 단어별로 토크나이징해서 sub_list에 추가, 조건은 명사이고 길이가 2글자 이상",
            "# word_list 에는 모든 단어들이 들어감",
            "# word를 counter 로 변환하여 워드 클라우드 생성",
            "import pandas as pd",
            "from konlpy.tag import Okt ",
            "import re ",
            "from collections import Counter ",
            "from wordcloud import WordCloud ",
            "import matplotlib.pyplot as plt",
            "import koreanize_matplotlib",
            "# Data를 클랜징하여 text,score 형대로 만든다. index를 제거한다",
            "df = pd.read_csv(",
            "    \"$1\", #./data/datafile.csv",
            "    index_col=0",
            ")",
            "",
            "okt = Okt()",
            "# 반복문",
            "word_list = []",
            "stopwords = [\"진짜\", \"카톡\", \"로그인\"]",
            "",
            "# 패턴: [^0-9가-힣a-zA-Z\\s]",
            "for sent in df[\"text\"]: # sent: 문장",
            "    # print(\"STEP1. 문장을 전처리합니다.\")",
            "    # 리뷰 텍스트 전처리",
            "    # 패턴: \"[^0-9a-zA-Zㄱ-ㅎㅏ-ㅣ가-힣\\s]\" : 숫자, 영어 소문자, 영어 대문자, 자음, 모음, 한글, 띄어쓰기가 아닌 것",
            "    clean_sent = re.sub(\"[^0-9가-힣a-zA-Z\\\\s]\", \"\", sent)",
            "    # print(\"STEP2. 문장을 형태소분석기로 토크나이징합니다.\")",
            "    # 토크나이징(형태소 분석기: Okt)",
            "    result = okt.pos(clean_sent)",
            "    # result를 하나씩 뽑는다. ",
            "    # print(\"STEP3. 새로운 리스트를 만듭니다.\")",
            "    sub_list = []",
            "    # print(\"\\t탐색을 시작합니다.\")",
            "    for res in result: # res : (단어, 품사)",
            "        word = res[0]",
            "        pos = res[1]",
            "        # word가 stopwords에 있으면 건너뛰기",
            "        if word in stopwords:",
            "            # print(\"\\t건너뛰어!\", res)",
            "            continue",
            "",
            "        # sub_list 만들기: 조건 pos == \"Noun\" and len(word) > 1",
            "        if pos == \"Noun\" and len(word) > 1:",
            "            sub_list.append(word)",
            "word_list.extend(sub_list)",
            "print(f\"\\t[WORD LIST] {word_list}\")",
            "print(\"=\"*100)",
            "    ",
            "",
            "counter = Counter(word_list)",
            "wc = WordCloud(",
            "    font_path = \"C:\\\\Windows\\\\Fonts\\\\malgun.ttf\", # \"/usr/share/fonts/truetype/nanum/NanumGothic.ttf\"",
            "    background_color=\"white\",",
            "    width=800,",
            "    height=400",
            ")",
            "",
            "wc.generate_from_frequencies(counter)",
            "",
            "plt.figure(figsize=(5,5))",
            "plt.imshow(wc, interpolation=\"bilinear\")",
            "plt.axis(\"off\")",
            "plt.title(\"배달의 민족 워드 클라우드(명사)\", fontsize=15)",
            "plt.show()"
        ],
        "description": "한국어 텍스트 데이터 분석 및 워드 클라우드 생성"
    },
    "Python_Bar_Chart": {
        "prefix": "pythonBarChart",
        "body": [
            "#import pandas as pd",
            "#",
            "#import matplotlib.pyplot as plt",
            "#import koreanize_matplotlib",
            "#",
            "#df = pd.read_csv(",
            "#    \"./data/appreply2.csv\", # \"/content/appreply2.csv\",",
            "#    index_col=0",
            "#)",
            "#df",
            "",
            "df[\"$1\"].unique()",
            "$1_summary = df[\"$1\"].value_counts().sort_index()",
            "$1_summary",
            "",
            "plt.figure(figsize=(5,3))",
            "$1_summary.plot(kind=\"bar\")",
            "plt.title(\"$1 막대그래프\")",
            "plt.ylabel(\"Y축라벨\")",
            "plt.xticks(rotation=0) # x 축 라벨 회전시키기",
            "plt.show()"
        ],
        "description": "데이터프레임 컬럼 분석 및 막대그래프 생성"
    },
    "Python_Hist_chart": {
        "prefix": "pythonHistChart",
        "body": [
            "#import pandas as pd",
            "#import matplotlib.pyplot as plt",
            "#import koreanize_matplotlib",
            "#",
            "#df = pd.read_csv(",
            "#    \"./data/appreply2.csv\", # \"/content/appreply2.csv\",",
            "#    index_col=0",
            "#)",
            "#df",
            "df[\"$1\"] = df[\"text\"].apply(lambda x: len(x))",
            "df.sort_values(by=[\"$1\"], ascending=False)",
            "plt.figure(figsize=(5,3))",
            "plt.hist(df[\"$1\"], bins=30, edgecolor=\"black\")",
            "plt.title(\"$1 히스토그램\")",
            "plt.xlabel(\"$1\")",
            "plt.ylabel(\"Y축라벨\")",
            "plt.show()"
        ],
        "description": "데이터프레임 컬럼 분석 및 히스토그램 생성"
    },
    "Python_Hitmap": {
        "prefix": "pythonHeatmap",
        "body": [
            "# import pandas as pd",
            "# import koreanize_matplotlib # 한글 적용",
            "# import matplotlib.pyplot as plt",
            "# import seaborn as sns",
            "",
            "df2 = pd.read_csv(\"$1\")",
            "print(df2.head(2))",
            "",
            "# 성별 X 연령대 분포에 따른 건당 이용시간",
            "index = \"$2\"",
            "columns = \"$3\"",
            "values = \"$4\"",
            "pivot_table = df2.pivot_table(",
            "    values=values,    # Value 컬럼",
            "    index=index,      # Y축 컬럼",
            "    columns=columns,  # X축 컬럼",
            "    aggfunc=\"mean\"",
            ")",
            "",
            "print(pivot_table)",
            "sns.heatmap(",
            "    pivot_table,",
            "    annot=True,",
            "    fmt=\".0f\",",
            "    cmap=\"YlOrRd\"",
            ")",
            "plt.title(f\"{index} × {columns} 분포에 따른 {values}\", fontsize=16)",
            "plt.xlabel(\"$2\")",
            "plt.ylabel(\"$3\")",
            "plt.show()"
        ],
        "description": "피벗 테이블 및 히트맵 생성 - $1: CSV 파일 경로, $2: index (Y축), $3: columns (X축), $4: values (값)"
    },
    "Python_Pairplot": {
        "prefix": "pythonPairplot",
        "body": [
            "# import pandas as pd",
            "# import matplotlib.pyplot as plt",
            "# import seaborn as sns",
            "# import koreanize_matplotlib",
            "",
            "sns.pairplot(",
            "    df[['$1', '$2']],",
            "    diag_kind='hist'",
            ")",
            "plt.show()"
        ],
        "description": "이상치 파악을 위해 Pairplot 생성 - $1: 첫 번째 컬럼명, $2: 두 번째 컬럼명"
    },
    "Python_Requests_data.seoul.go.kr": {
        "prefix": "pythonRequests_data.seoul.go.kr",
        "body": [
            "import os",
            "import requests",
            "from dotenv import load_dotenv",
            "load_dotenv()",
            "",
            "base_url = \"$1\"",
            "key = os.getenv(\"$2\")",
            "request_type = \"json\"",
            "service = \"$3\"",
            "start_index = 1",
            "end_index = 5",
            "step = 1000",
            "",
            "url = f\"{base_url}/{key}/{request_type}/{service}/{start_index}/{end_index}\"",
            "",
            "response = requests.get(url)",
            "data = response.json()",
            "print(data[\"$4\"].keys())",
            "items = data.get('$4').get('row')",
            "list_total_count = data.get('$4').get('list_total_count')",
            "print(list_total_count)",
            "",
            "items_all = []",
            "request_count = (int(list_total_count) // step) + 1",
            "print(request_count)",
            "",
            "for i in range(request_count):",
            "    start_idx = i * step + 1",
            "    end_idx = (i + 1) * step",
            "    uri = f\"{base_url}/{key}/{request_type}/{service}/{start_idx}/{end_idx}\"",
            "    print(uri)",
            "    response = requests.get(uri)",
            "    data = response.json()",
            "    items = data.get('$4').get('row')",
            "    items_all.extend(items)",
            "",
            "print(f\"\\n전체 데이터 개수: {len(items_all)}\")",
            "",
            "print(\"\\n처음 5개 데이터:\")",
            "for item in items_all[:5]:",
            "    print(item)"
        ],
        "description": "Open API 데이터 수집\n$1: base_url\n$2: API_KEY 환경변수명\n$3: service명\n$4: 데이터 키"
    },
    "Python_Requests_data.go.kr": {
        "prefix": "pythonRequests_data.go.kr",
        "body": [
            "from dotenv import load_dotenv",
            "import os",
            "import requests",
            "import math",
            "",
            "load_dotenv()",
            "",
            "base_url = \"$1\"",
            "",
            "# 요청파라미터",
            "serviceKey = os.getenv(\"$2\")",
            "searchYearCd = $3",
            "siDo = $4",
            "guGun = $5",
            "request_type = \"json\"",
            "numOfRows = 10",
            "pageNo = 1",
            "",
            "# base_url?요청파라미터1=값&요청파라미터2=값&....",
            "url = f\"{base_url}?ServiceKey={serviceKey}&searchYearCd={searchYearCd}&siDo={siDo}&guGun={guGun}&type={request_type}&numOfRows={numOfRows}&pageNo={pageNo}\"",
            "print(url)",
            "",
            "response = requests.get(url)",
            "print(response.status_code)",
            "",
            "data = response.json()",
            "print(data.keys())",
            "",
            "if (data.get('resultCode') != '00'):",
            "    print(\"Error: \", data.get('resultMsg'))",
            "    exit()",
            "",
            "totalCount = data.get('totalCount')",
            "print(totalCount)",
            "",
            "",
            "",
            "# 전체 데이터를 담을 리스트",
            "item_list = []",
            "",
            "# 필요한 페이지 수 계산 (totalCount / numOfRows를 올림)",
            "total_pages = math.ceil(totalCount / numOfRows)",
            "print(f\"총 {totalCount}개의 데이터를 {total_pages}페이지에 걸쳐 수집합니다.\")",
            "",
            "# 각 페이지를 반복하며 데이터 수집",
            "for page in range(1, total_pages + 1):",
            "    # URL 생성",
            "    url = f\"{base_url}?ServiceKey={serviceKey}&searchYearCd={searchYearCd}&siDo={siDo}&guGun={guGun}&type={request_type}&numOfRows={numOfRows}&pageNo={page}\"",
            "    ",
            "    # API 요청",
            "    response = requests.get(url)",
            "    ",
            "    # 응답 확인",
            "    if response.status_code == 200:",
            "        data = response.json()",
            "        ",
            "        # 에러 체크",
            "        if data.get('resultCode') == '00':",
            "            items = data.get('items', {}).get('item', [])",
            "            item_list.extend(items)",
            "            print(f\"페이지 {page}/{total_pages}: {len(items)}개 수집 완료\")",
            "        else:",
            "            print(f\"페이지 {page} 에러: {data.get('resultMsg')}\")",
            "    else:",
            "        print(f\"페이지 {page} 요청 실패: {response.status_code}\")",
            "    ",
            "print(f\"\\n최종적으로 총 {len(item_list)}개의 데이터를 수집했습니다.\")",
            "",
            "import pandas as pd",
            "",
            "# 데이터프레임으로 변환",
            "df = pd.DataFrame(item_list)",
            "df.head()",
            "",
            "# 데이터 저장",
            "df.to_csv(",
            "    \"$6\",",
            "    index=False,",
            "    encoding=\"utf-8-sig\"",
            ")",
            "print(\"데이터 저장 완료!\")"
        ],
        "description": "공공데이터포털 API 데이터 페이징 수집\n$1: base_url\n$2: API_KEY 환경변수명\n$3: searchYearCd\n$4: siDo\n$5: guGun\n$6: 저장 파일 경로"
    },
    "Python_Youtube_commentThreads": {
        "prefix": "pythonYoutubeCommentThreads",
        "body": [
            "from dotenv import load_dotenv",
            "import os",
            "import requests",
            "",
            "load_dotenv()",
            "",
            "base_url = \"https://www.googleapis.com/youtube/v3/commentThreads\"",
            "",
            "part = \"snippet\"",
            "videoId = \"$1\"                # 댓글 수집할 영상",
            "key = os.getenv(\"YOUTUBE_API_KEY\")",
            "maxResults = 1000                       # 최대 1000개",
            "textFormat = \"plainText\"",
            "",
            "url = f\"{base_url}?part={part}&videoId={videoId}&key={key}&maxResults={maxResults}&textFormat={textFormat}\"",
            "print(url)",
            "",
            "response = requests.get(url)",
            "",
            "data = response.json()",
            "print('pageInfo.totalResults', data.get('pageInfo').get('totalResults'));",
            "totalResults = data.get('pageInfo').get('totalResults')",
            "resultsPerPage = data.get('pageInfo').get('resultsPerPage')",
            "nextPageToken = data.get('nextPageToken')",
            "",
            "print(f\"총 댓글 수: {totalResults}\")",
            "print(f\"페이지당 결과 수: {resultsPerPage}\")",
            "print()",
            "",
            "# 전체 데이터를 담을 리스트",
            "item_list = []",
            "",
            "# 첫 번째 페이지 데이터 추가",
            "if 'items' in data:",
            "    items = data['items']",
            "    item_list.extend(items)",
            "    print(f\"페이지 1: {len(items)}개 수집 완료 (총 {len(item_list)}개)\")",
            "",
            "# 다음 페이지가 있는 동안 반복",
            "page_count = 1",
            "while nextPageToken:",
            "    page_count += 1",
            "",
            "    ",
            "    # nextPageToken을 포함한 URL 생성",
            "    url = f\"{base_url}?part={part}&videoId={videoId}&key={key}&maxResults={maxResults}&textFormat={textFormat}&pageToken={nextPageToken}\"",
            "    ",
            "    # API 요청",
            "    response = requests.get(url)",
            "    ",
            "    # 응답 확인",
            "    if response.status_code == 200:",
            "        data = response.json()",
            "        ",
            "        # items 추출 및 추가",
            "        if 'items' in data:",
            "            items = data['items']",
            "            item_list.extend(items)",
            "            print(f\"페이지 {page_count}: {len(items)}개 수집 완료 (총 {len(item_list)}개)\")",
            "        ",
            "        # 다음 페이지 토큰 업데이트",
            "        nextPageToken = data.get('nextPageToken')",
            "        ",
            "        # nextPageToken이 없으면 종료",
            "        if not nextPageToken:",
            "            print(\"더 이상 수집할 페이지가 없습니다.\")",
            "            break",
            "    else:",
            "        print(f\"페이지 {page_count} 요청 실패: {response.status_code}\")",
            "        if response.status_code == 403:",
            "            print(\"API 할당량을 초과했거나 권한이 없습니다.\")",
            "        break",
            "",
            "print(f\"\\n최종적으로 총 {len(item_list)}개의 댓글을 수집했습니다.\")",
            "",
            "# 데이터프레임으로 변환",
            "import pandas as pd",
            "",
            "# 댓글 데이터 추출",
            "comment_data = []",
            "for item in item_list:",
            "    snippet = item['snippet']['topLevelComment']['snippet']",
            "    comment_data.append({",
            "        'author': snippet['authorDisplayName'],",
            "        'comment': snippet['textDisplay'],",
            "        'likeCount': snippet['likeCount'],",
            "        'publishedAt': snippet['publishedAt']",
            "    })",
            "",
            "df = pd.DataFrame(comment_data)",
            "print(df.head())",
            "",
            "# 데이터 저장",
            "df.to_csv(",
            "    f\"./data/유튜브_댓글_{videoId}_(260127).csv\",",
            "    index=False,",
            ")",
            "print(\"데이터 저장 완료!\")"
        ],
        "description": "YouTube API를 사용한 댓글 수집 및 CSV 저장\n$1: videoId (댓글 수집할 유튜브 영상 ID)"
    },
    "Python_Naver_New_Search": {
        "prefix": "pythonNaverNewSearch",
        "body": [
            "from dotenv import load_dotenv",
            "import pandas as pd",
            "import requests",
            "import os",
            "from datetime import datetime",
            "",
            "load_dotenv()",
            "",
            "# 설정",
            "BASE_URL = \"https://openapi.naver.com/v1/search/news.json\"",
            "QUERY = \"$1\"",
            "DISPLAY = 100",
            "SORT = \"sim\"",
            "",
            "# API Key 확인",
            "client_id = os.getenv(\"X-Naver-Client-Id\")",
            "client_secret = os.getenv(\"X-Naver-Client-Secret\")",
            "",
            "if not client_id or not client_secret:",
            "    print(\"❌ 네이버 API Key가 설정되지 않았습니다.\")",
            "    exit()",
            "",
            "# 요청 파라미터",
            "params = {",
            "    \"query\": QUERY,",
            "    \"display\": DISPLAY,",
            "    \"start\": 1,",
            "    \"sort\": SORT",
            "}",
            "",
            "# 헤더",
            "headers = {",
            "    \"X-Naver-Client-Id\": client_id,",
            "    \"X-Naver-Client-Secret\": client_secret",
            "}",
            "",
            "# API 요청",
            "try:",
            "    response = requests.get(BASE_URL, params=params, headers=headers, timeout=10)",
            "    response.raise_for_status()",
            "except requests.exceptions.HTTPError:",
            "    print(f\"❌ API 요청 실패: {response.status_code}\")",
            "    if response.status_code == 400:",
            "        print(\"잘못된 요청입니다.\")",
            "    elif response.status_code == 401:",
            "        print(\"인증 실패. API Key를 확인하세요.\")",
            "    elif response.status_code == 403:",
            "        print(\"API 할당량 초과 또는 권한 없음\")",
            "    exit()",
            "except requests.exceptions.RequestException as e:",
            "    print(f\"❌ 네트워크 오류: {e}\")",
            "    exit()",
            "",
            "# JSON 파싱",
            "try:",
            "    data = response.json()",
            "except ValueError as e:",
            "    print(f\"❌ JSON 파싱 오류: {e}\")",
            "    exit()",
            "",
            "# 데이터 추출",
            "item_list = data.get(\"items\", [])",
            "total_count = data.get(\"total\", 0)",
            "",
            "print(f\"📊 검색 결과: 총 {total_count:,}건\")",
            "print(f\"✅ 수집 완료: {len(item_list)}개\\n\")",
            "",
            "# 데이터프레임 생성",
            "df = pd.DataFrame(item_list)",
            "print(\"📋 데이터 미리보기:\")",
            "print(df.head())",
            "print(f\"\\n📈 데이터 통계: {len(df):,}행 × {len(df.columns)}열\\n\")",
            "",
            "# 디렉토리 생성",
            "os.makedirs('./data', exist_ok=True)",
            "",
            "# 데이터 저장",
            "today = datetime.now().strftime('%y%m%d')",
            "filename = f\"./data/네이버_뉴스_{QUERY}_({today}).csv\"",
            "df.to_csv(filename, index=False, encoding='utf-8-sig')",
            "print(f\"💾 데이터 저장 완료: {filename}\")"
        ],
        "description": "네이버 뉴스 검색 API 데이터 수집 및 CSV 저장\n$1: 검색 키워드"
    },
    "Python_File_Merge": {
        "prefix": "PythonFileMerge",
        "body": [
            "import pandas as pd",
            "import folium",
            "from datetime import datetime",
            "",
            "",
            "",
            "file1 = \"$1\"",
            "file2 = \"$2\"",
            "myKey = '$3'",
            "",
            "df = pd.read_csv(file1)",
            "print(df.head(2))",
            "",
            "loc_df = pd.read_csv(file2)",
            "print(loc_df.head(2))",
            "",
            "# file1 key colums...",
            "file1_colums = ['$4', '$5', '$6']",
            "file1_df_new = df[[myKey] + file1_colums]",
            "",
            "print(file1_df_new)",
            "",
            "# file2 key colums...",
            "file2_key = '$7'",
            "file2_colums = ['$8', '$9']",
            "file2_df_new = loc_df[ [file2_key] + file2_colums ]",
            "file2_df_new.columns = [myKey, \"LAT\", \"LONG\"]",
            "print(file2_df_new)",
            "",
            "merge_data = pd.merge(",
            "    left=file1_df_new,          # 데이터1",
            "    right=file2_df_new,         # 데이터2",
            "    how=\"left\",                 # 취합방법(left, right, inner, outer)",
            "    on=myKey                    # 기준",
            ")",
            "print(merge_data)",
            "",
            "today = datetime.now().strftime('%y%m%d')",
            "filename = f\"./data/merged_{myKey}_{today}.csv\"",
            "merge_data.to_csv(filename, index=False, encoding='utf-8-sig')",
            "print(f\"데이터 저장 완료: {filename}\")"
        ],
        "description": "두 개의 CSV 파일을 병합하고 저장\n$1: file1 경로\n$2: file2 경로\n$3: 병합 키\n$4-$6: file1 컬럼들\n$7: file2 키\n$8-$9: file2 컬럼들"
    },
}